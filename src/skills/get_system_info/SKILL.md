# get_system_info

Returns the current date, time, timezone, and host platform details.
Use this whenever the user asks about the current time, date, or the machine's environment.

## Schema

```json
{
  "name": "get_system_info",
  "description": "Returns the current date, time, timezone, and host platform. Use whenever the user asks about the current time, date, or the machine environment.",
  "parameters": {
    "type": "object",
    "properties": {},
    "required": []
  }
}
```
