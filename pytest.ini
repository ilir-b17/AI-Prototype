[pytest]
asyncio_mode = auto
testpaths = tests
markers =
	slow: slow eval requiring Ollama (deselect with '-m "not slow"')
	tier1: deterministic eval, no IO
	tier2: heuristic eval, SkillRegistry only
	tier3: LLM-graded eval, requires Ollama
